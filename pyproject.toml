[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "novaql"
version = "0.1.0"
description = "NovaQL: Next-gen piped query language surpassing SQL alternatives"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["lark>=1.1.9", "duckdb>=1.0.0"]
